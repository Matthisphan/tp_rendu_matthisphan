{% extends 'base.html.twig' %}

{% block title %}Admin - Language{% endblock %}

{% block body %}
<div class="containerPosition">
    <div class="containerLeftMenu">
        {% include 'parts/leftMenu.html.twig' %}
    </div>

    <div class="containerMainArticleShow">
        <div class="admin-container">
            <h1 class="page-title">Language Details</h1>

            <div class="card">
                <table class="table">
                    <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ language.id }}</td>
                    </tr>
                    <tr>
                        <th>Name</th>
                        <td>{{ language.name }}</td>
                    </tr>
                    <tr>
                        <th>Code</th>
                        <td>{{ language.code }}</td>
                    </tr>
                    </tbody>
                </table>
            </div>

            <div class="action-buttons">
                <a href="{{ path('app_admin_langugage_index') }}" class="btn back-btn">Back to list</a>
                <a href="{{ path('app_admin_langugage_edit', {'id': language.id}) }}" class="btn edit-btn">Edit</a>
            </div>

            {{ include('admin/langugage/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
